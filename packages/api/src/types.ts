export enum EventAction {
  Connect = '$connect',
  Disconnect = '$disconnect',
  Default = '$default',
  Subscribe = 'subscribe',
  Unsubscribe = 'unsubscribe',
}

export enum Subscription {
  Time = 'time',
  Location = 'location',
}
